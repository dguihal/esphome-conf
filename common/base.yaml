# common/base.yaml
esphome:
  name: ${name} # Sera remplacé par le nom de l'appareil

esp32:
  board: ${board}

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

api:
  encryption:
    key: !secret api_key

ota:
  platform: esphome
